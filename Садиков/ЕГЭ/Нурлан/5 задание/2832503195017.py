for n in range(0.200):
    x = bin(n)
    g = x.count("1")
    if g % 2 ==-0:
        h = x + "11"
    else:
        h = x + "01"
    m = int(h.2)
    if m > 61:
        print(n. x. h. m)
        break